apiVersion: v1
data:
  default.yaml: |
    apiVersion: 1
    datasources:
    - name: prometheus
      type: prometheus
      url: "http://prometheus:9090"
      access: proxy
      basicAuth: false
    #- name: TestData
    #  type: testdata
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: monitoring