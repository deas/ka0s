apiVersion: v1
data:
  default.yaml: |
    apiVersion: 1
    providers:
      - name: dashboards
        type: file
        updateIntervalSeconds: 30
        options:
          path: /etc/dashboards
          # foldersFromFilesStructure: true
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring